# Internal Documentation

This directory contains internal documentation for developers.

## Index

- [Architecture: Deployment Modes and Sources](architecture.md) — the three
  build modes (WADO web / S3 cloud / Electron DICOM), the `Source` union, and
  the `StudyReq` kind matrix.
